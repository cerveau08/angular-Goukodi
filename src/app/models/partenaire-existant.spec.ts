import { PartenaireExistant } from './partenaire-existant';

describe('PartenaireExistant', () => {
  it('should create an instance', () => {
    expect(new PartenaireExistant()).toBeTruthy();
  });
});
